# 移动端加载与设置布局修复方案

## 问题结论

- 移动端生产包不能依赖 Electron 本地 Node API 服务，也不应要求额外部署在线代理服务。
- 首页推荐区把 `loading` 固定为 `true`；请求失败后没有错误或重试状态，因此一直显示骨架屏。
- 设置项在窄屏下仍采用左右双列，并为控件固定保留 `140px`，导致标题和描述逐字换行。

## 修复范围

1. 在 App 内加入移动端网易云请求适配层，通过 Capacitor 原生 HTTP 直连网易云上游，不依赖自建或第三方 API 服务。
2. 首页推荐增加独立的加载、失败和重试状态；请求完成后关闭骨架屏，部分接口失败时保留已成功内容。
3. 设置项在手机宽度下改为稳定的文字区与控件区布局：开关保持右侧固定宽度，长描述正常按词句换行；选择框和输入框在必要时换到下一行并占满宽度。
4. 保持桌面端现有布局，不改变 Naive UI 组件体系。

## 内置请求结构

```text
Vue API wrappers
      |
Mobile NCM route adapter
      |
Capacitor native HTTP
      |
NetEase upstream endpoints
```

- 复用现有网易云 API 依赖中的纯路由参数映射，不在 WebView 中启动 Node.js。
- App 内实现 `weapi`、`eapi` 和普通 `api` 请求，并保存登录响应 Cookie。
- Electron 与开发服务器继续使用现有本地 API，不改变桌面端行为。
- 网易云上游并非稳定公开 API，若上游协议或风控调整，需要随 App 版本更新适配层。

## 移动端布局示意

```text
┌──────────────────────────────┐
│ 显示搜索历史             [开关] │
│ 是否在搜索框的默认显示内容中     │
│ 显示当前搜索历史                 │
└──────────────────────────────┘

┌──────────────────────────────┐
│ 搜索框行为                     │
│ 自定义搜索框的行为模式           │
│ [ 保留搜索词                 ▾ ] │
└──────────────────────────────┘
```

首页请求失败时：

```text
推荐歌单
┌──────────────────────────────┐
│ 在线内容加载失败                │
│              [重新加载]         │
└──────────────────────────────┘
```

## 验证

- `pnpm lint`
- `pnpm typecheck`
- `pnpm build`
- GitHub Actions Android APK 和 iOS Simulator 构建
- 手机宽度截图检查首页、设置开关项、选择项和输入项
